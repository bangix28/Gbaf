{% extends 'layout.twig' %}
{% block head %}
    <title>Acceuil du gbaf </title>
{% endblock %}
{% block content %}
<div class="col-lg-12">
    <section>
        <div class="col-lg-12">
          <!-- Uncomment when  separator are find.  <img src=""> !-->
        </div>
        <div class="col-12">
            <h1 class="leads text-center">Lorem ipseum......</h1>
        </div>
            <div class="col-12">
            <!-- Uncomment when  separator are find.  <img src=""> !-->
        </div>
    </section>
    <section class="container p-5">

        {% for data in listActors %}

        <article class="offset-2 col-lg-10 justify-content-center p-4">
            <div class="row">
                <div class="col-4">
                    <img style="max-width: 150px;" src="images/upload_images/{{ data.logo }}">
                </div>
                <div class="col-lg-6">
                    <h3><a href="{{ data.link }}"></a>{{ data.description }}</h3>
                </div>
                <a href="index.php?access=actor&amp;id={{ data.actor_id }}"><p class="lead">Lire la suite</p></a>
            </div>

        </article>
        {% endfor %}
        <div class="col-lg-12">
            <!-- Uncomment when  separator are find.  <img src=""> !-->
        </div>
    </section>
</div>
{% endblock %}


